version: '3.9'

services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4545:4545"
    environment:
      - NODE_ENV=development
        DB='mongodb+srv://workspraneel:YwGZNiGEH5rTgiXl@supra.uktz3kt.mongodb.net/?retryWrites=true&w=majority&appName=Supra'
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules

  frontend:
    image: frontend-image
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5174:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/usr/src/app/
      - /usr/src/app/node_modules
      