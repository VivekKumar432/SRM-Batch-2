version: '3.9'

services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4040:4040"
    environment:
      - NODE_ENV=development
        DB='mongodb+srv://piebytwo014:admin123@batch2.axgnzwf.mongodb.net/?retryWrites=true&w=majority&appName=batch2'
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules

  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/usr/src/app/
      - /usr/src/app/node_modules